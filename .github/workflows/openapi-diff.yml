# +-------------------------------------------------------------+
# |                          CERTEUS                            |
# +-------------------------------------------------------------+
# | FILE: .github/workflows/openapi-diff.yml                  |
# | ROLE: Project YAML manifest.                                |
# | PLIK: .github/workflows/openapi-diff.yml                  |
# | ROLA: Manifest YAML projektu.                               |
# +-------------------------------------------------------------+

name: openapi-diff

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Diff placeholder
        run: |
          echo "OpenAPI diff placeholder"

